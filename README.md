# ClarityBudget
A structured personal finance data pipeline that cleans transactions, applies category logic, generates yearly metrics, and visualizes insights through a dynamic dashboard. Uses mock data to showcase ETL, modeling, and automation best practices.
